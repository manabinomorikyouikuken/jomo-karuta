node_modules
dist
.DS_Store
docs/提出用キャプチャ_2026-08/

# docs/ は内部文書のため原則非公開（public リポジトリ）
# 個別に公開する場合は下に ! で明示すること。
# 注意: docs/ ではなく docs/* と書くこと。
# ディレクトリごと除外すると ! による個別許可が効かなくなる。
docs/*

# CLAUDE.md は作業用の内部メモ。許諾の経緯・未確定事項・ローカルパスを含むため
# ローカルに置き、公開しない。Claude Code は追跡外でもディスクから読む。
CLAUDE.md
